<%= form_with(model: publictchat, remote: true) do |form| %>
  <% if publictchat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(publictchat.errors.count, "error") %> prohibited this publictchat from being saved:</h2>

      <ul>
      <% publictchat.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :message %>
    <%= form.text_area :message %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
